user nginx;
events {
worker_connections 1000;
}

http {
	upstream facility_service {
		server facilityserviceapi:8080;
}
	server {
		listen 4000;
		location /facility/ {
			proxy_pass http://facility_service;
		}
	}
}