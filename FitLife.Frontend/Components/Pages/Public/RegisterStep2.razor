@page "/register-step2"
@rendermode InteractiveServer
@using FitLife.Frontend.Models
@using FitLife.Frontend.Services
@inject CenterService centerService
@inject MemberService memberService
@inject NavigationManager navman

<div class="container-fluid px-0">

    <!-- Shared navbar for public pages -->
    <PublicNavbar />

    <section class="container py-5">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Opret medlemskab</h2>

            <!-- Prototype-knap -->
            <!-- TODO: Kan senere åbne support/chat/hjælpesektion -->
            <button class="btn btn-outline-secondary" @onclick="ShowNotImplemented">
                Hjælp mig
            </button>
        </div>

        <!-- Step-indikator -->
        <div class="row mb-4 text-center">
            <div class="col">
                <div class="border rounded p-3 bg-light">
                    Trin 1<br />
                    Personlige informationer
                </div>
            </div>

            <div class="col">
                <div class="border rounded p-3 bg-success text-white">
                    Trin 2<br />
                    Vælg medlemskabstype
                </div>
            </div>

            <div class="col">
                <div class="border rounded p-3 bg-light">
                    Trin 3<br />
                    Betalingsoplysninger
                </div>
            </div>
        </div>

        <div class="row g-4">

            <!-- Valg af center og medlemskab -->
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-body p-4">

                        <h4 class="mb-3">Vælg center</h4>

                        <!-- Midlertidig dropdown med centre -->
                        <!-- TODO: Senere skal centre hentes fra FacilityService/backend -->
                        <select class="form-select mb-4"
                                @bind="SelectedCenterID">

                            <option value="">Vælg center</option>

                            @foreach (var center in centers)
                            {
                                <option value="@center.Id">
                                    @center.Name
                                </option>
                            }
                        </select>

                        <h4 class="mb-3">Vælg medlemskabstype</h4>

                        <!-- Midlertidige medlemskabstyper -->
                        <!-- TODO -->
                        <div class="list-group mb-4">

                            @foreach (var membershipType in MembershipTypes)
                            {
                                <button type="button"
                                        class="list-group-item list-group-item-action @(SelectedMembershipType == membershipType ? "active" : "")"
                                        @onclick="() => SelectMembership(membershipType)">

                                    <div class="d-flex justify-content-between">
                                        <span>@membershipType.Name medlemskab</span>

                                        <strong>
                                            @membershipType.MonthlyPrice kr./md.
                                        </strong>
                                    </div>

                                    <small>
                                        @membershipType.Description
                                    </small>

                                </button>
                            }
                        </div>

                        <h4 class="mb-3">Vælg tilvalg</h4>

                        <!-- TODO -->
                        <div class="form-check">
                            <input class="form-check-input"
                                   type="checkbox"
                                   @bind="PersonalTrainingAddon" />

                            <label class="form-check-label">
                                Personlig træning +150 kr./md.
                            </label>
                        </div>

                        <div class="form-check">
                            <input class="form-check-input"
                                   type="checkbox"
                                   @bind="DigitalTrainingAddon" />

                            <label class="form-check-label">
                                Digital træning +50 kr./md.
                            </label>
                        </div>

                        @if (!string.IsNullOrWhiteSpace(Message))
                        {
                            <div class="alert alert-warning mt-4">
                                @Message
                            </div>
                        }

                    </div>
                </div>
            </div>

            <!-- Oversigt over brugerens valg -->
            <div class="col-md-4">
                <div class="card shadow-sm">
                    <div class="card-body p-4">

                        <h4>Dine valg</h4>

                        <p>
                            <strong>Center:</strong><br />
                            @(string.IsNullOrWhiteSpace(SelectedCenterID)
                                ? "Ikke valgt"
                                : SelectedCenterID)
                        </p>

                        <p>
                            <strong>Valgt medlemskabstype:</strong><br />
                            @(SelectedMembershipType is null
                                ? "Ikke valgt"
                                : SelectedMembershipType.Name)
                        </p>

                        <p>
                            <strong>Valgte tilvalg:</strong><br />

                            @if (!PersonalTrainingAddon && !DigitalTrainingAddon)
                            {
                                <span>Ingen tilvalg</span>
                            }
                            else
                            {
                                @if (PersonalTrainingAddon)
                                {
                                    <span>Personlig træning</span>
                                    <br />
                                }

                                @if (DigitalTrainingAddon)
                                {
                                    <span>Digital træning</span>
                                }
                            }
                        </p>

                        <hr />

                        <h5>Total pris pr. måned</h5>

                        <h3>@TotalPrice kr.</h3>

                    </div>
                </div>
            </div>

        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="/register-step1"
               class="btn btn-outline-secondary">
                Tilbage
            </a>

            <button class="btn btn-success px-4"
                    @onclick="GoToNextStep">
                Næste
            </button>
        </div>

    </section>
    @if (!string.IsNullOrWhiteSpace(Message))
    {
        <div class="alert alert-info mt-4">
            @Message1
        </div>
    }
    <AppFooter />

</div>

@code {
    // Gemmer navnet på det valgte center.
    // TODO: Senere skal dette mappes til en rigtig Affiliation Guid fra FacilityService.
    private string SelectedCenterID = "";

    // Gemmer det valgte medlemskab i frontend.
    private MembershipType? SelectedMembershipType;
    private string SelectedMembershipTypeString = "standard";

    // Prototype-tilvalg.
    // TODO: Disse skal senere afklares med backend-modellen.
    private bool PersonalTrainingAddon;

    private bool DigitalTrainingAddon;

    private string Message = "";
    private string Message1;

    private List<ExerciseGym> centers = new();

    private List<MembershipType> MembershipTypes = new()
    {
        new MembershipType
        {
            Name = "Standard",
            Description = "Adgang til center og basis træningsfaciliteter.",
            MonthlyPrice = 199
        },

        new MembershipType
        {
            Name = "Premium",
            Description = "Adgang til center, holdtræning og digitale træningsprogrammer.",
            MonthlyPrice = 299
        }
    };

    protected override async Task OnInitializedAsync()
    {
        centers = await centerService.GetCentersAsync();
        SelectedCenterID = memberService.DraftMember.Affiliation ?? "";
    }

    private decimal TotalPrice =>
        (SelectedMembershipType?.MonthlyPrice ?? 0) +
        (PersonalTrainingAddon ? 150 : 0) +
        (DigitalTrainingAddon ? 50 : 0);

    private void SelectMembership(MembershipType membershipType)
    {
        SelectedMembershipType = membershipType;

        SelectedMembershipTypeString = membershipType.Name.ToLower();
    }

    private void GoToNextStep()
    {
        if (string.IsNullOrWhiteSpace(SelectedCenterID))
        {
            Message = "Vælg et center.";
            return;
        }

        if (SelectedMembershipType is null)
        {
            Message = "Vælg en medlemskabstype.";
            return;
        }

        memberService.DraftMember.Affiliation = SelectedCenterID;

        memberService.DraftMember.MembershipType = SelectedMembershipType.Name;

        navman.NavigateTo("/register-step3");
    }
    private void ShowNotImplemented()
    {
        Message1 = "Denne funktion er ikke implementeret endnu.";
    }
}